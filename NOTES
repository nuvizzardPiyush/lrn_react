package.json
 every Node JS project has a package.json and it contains info about project, for example list of dependencies and scripts

package-lock.json
 snapshot of entire dependency tree

to create component we create a function(name starting with capital alphabet) which return html. adn we export the component